{
  "name": "root",
  "private": true,
  "scripts": {
    "build:server": "lerna run build --scope={@crispo/common,@crispo/server}"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "^3.8.4"
  }
}
